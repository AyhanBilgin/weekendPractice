public class QueueableAssignment implements Queueable {
    public void execute(QueueableContext context){
        case cs = new case();
        cs.status = 'New';
        cs.origin = 'Email';
        insert cs;
    }
}
